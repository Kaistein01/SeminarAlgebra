%
% teil1.tex -- Beispiel-File für das Paper
%
% (c) 2020 Prof Dr Andreas Müller, Hochschule Rapperswil
%
% !TEX root = ../../buch.tex
% !TEX encoding = UTF-8
%



% Hantelsymbol für vorwärtskorrelation
\newcommand{\laplacecorr}{%
  \mathrel{\circ}\joinrel\relbar\joinrel\relbar\joinrel\mathrel{\bullet}%
}

% Hantelsymbol für rückwärtskorrelation
\newcommand{\invlaplacecorr}{%
  \mathrel{\bullet}\joinrel\relbar\joinrel\relbar\joinrel\mathrel{\circ}%
}

\tikzset{
    ->-/.style={decoration={
        markings,
        mark=at position #1 with {\arrow{Stealth[length=3mm, width=2mm]}}},postaction={decorate}}
}