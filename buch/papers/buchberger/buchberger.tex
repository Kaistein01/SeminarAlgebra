%
% buchberger.tex
%
% (c) 2026 Nathanael Fässler, OST Ostschweizer Fachhochschule
%
% !TEX root = ../../buch.tex
% !TEX encoding = UTF-8
%
\section{Polynomielle Gleichungssysteme
\label{buchberger:section:buchberger}}
\kopfrechts{Polynomielle Gleichungssysteme}

Das Gleichungssystem
\begin{equation}
  \left\{\quad
  \renewcommand{\arraycolsep}{1.5pt}
  \begin{array}{lclcr}
    x^2 &-&y   &=& 0 \\
    x   &+&y^2 &=& 3
  \end{array}
  \right.
  \label{buchberger:polynom-gleichungssystem-1}
\end{equation}
lässt sich nicht mehr mit dem Gauss-Algorithmus lösen.
Der Grund ist, dass es Variablen mit Potenzen $> 1$ beinhaltet, also nicht linear ist.

Um solche nichtlinearen multivariaten\footnote{Multivariat bedeutet, dass mehrere Variablen vorkommen.} Gleichungssysteme zu lösen, eignet sich der Buch\-berger-Algorithmus.
Ein Gleichungssystem wie
\begin{equation}
  \left\{\quad
  \begin{aligned}
  5xy + 2y^3 +  5y^8z + z^3 &= 100 \\
  3x^2y^4z^9 + 2z^2 &= 1 \\
  (x-1)\,(y-2)\,(z-3) &= 0
  \end{aligned}
  \right.
  \label{buchberger:polynom-gleichungssystem-2}
\end{equation}
lässt sich damit auch lösen.
Terme mit mehreren Variablen wie $5xy$ sind hier im Gegensatz zu linearen Gleichungen erlaubt.
Die Klammern der letzten Gleichung stellen kein neues Problem dar, da sie beim Ausmultiplizieren auch in die bekannte Koeffizientenform gebracht werden können.

\subsection{Geschichte}
\label{buchberger:subsection:geschichte}
Im Jahre 1965 hat der österreichische Mathematikstudent Bruno Buchberger in seiner Dissertation die Gröbner-Basis entwickelt.
Er hat sie nach seinem Doktorvater Wolfgang Gröbner benannt.
Seine Dissertation enthält auch den Buchberger-Algorithmus, mit dem man Gröbner-Basen berechnen kann.
Der Algorithmus wurde zu einem wichtigen Bestandteil von Computeralgebrasystemen.

\subsection{Anwenden des Buchberger-Algorithmus}
\subsubsection{Input des Buchberger-Algorithmus vorbereiten}
\begin{beispiel}
Wie beim Gauss-Algorithmus müssen die Gleichungen zunächst in eine geeignete Form gebracht werden.
Die Gleichungen werden in Polynome umgewandelt, wobei sie vorher auf die Form $p(x) = 0$ umgestellt werden müssen.
Das Gleichungssystem
\begin{equation}
  \left\{\quad
  \renewcommand{\arraycolsep}{1.5pt}
  \begin{array}{lclcrcr}
  x^2 &-&y   & &  &=& 0 \\
  x   &+&y^2 &-&3 &=& 0
  \end{array}
  \right.
  \label{buchberger:polynom-gleichungssystem-3}
\end{equation}
wird in die Menge der Polynome 
\begin{equation}
  B
  =
  \{
    x^2 -y,
    x + y^2 - 3
  \}
  \label{buchberger:polynom-gleichungssystem-4}
\end{equation}
umgeformt.
Der Teil $= 0$ wird entfernt, aber immer noch impliziert, wenn man ein Polynom als Gleichung interpretiert.
Diese Umwandlung von Gleichungen zu Polynomen ist nötig, da man mit Polynomen einfacher rechnen kann als mit Gleichungen.
Der Output des Buchberger-Algorithmus ist auch eine Menge von Polynomen, welche dann zu einem Gleichungssystem rekonstruiert werden kann, indem an jedes Polynom das $= 0$ wieder angehängt wird.

Dabei ist wichtig, dass alle Polynome eine eindeutige Ordnung der Terme besitzen.
Zum Beispiel legt die lexikographische Ordnung fest, dass zuerst alle Terme mit dem höchsten Grad bezüglich $x$ stehen.
Haben mehrere Terme denselben Grad bezüglich $x$, wird innerhalb dieser Gruppe nach dem Grad von $y$ sortiert und dann bezüglich $z$ und so weiter bis zur letzten Variablen.
Dies ist nötig, da der Buchberger-Algorithmus eine spezielle Variante der Polynomdivision verwendet, wobei der führende Term bekannt sein muss.
Mehr zur Monomordnung ist in Abschnitt~\ref{buch:komplex:symmetrisch:subsection:fundamentalsatz} zu finden, wobei sie für den Fundamentalsatz für symmetrische Polynome eine Rolle spielt.
\end{beispiel}

\subsubsection{Buchberger-Algorithmus ausführen}
\begin{beispiel}
Wenn man den Buchberger-Algorithmus auf das System \eqref{buchberger:polynom-gleichungssystem-1} 
anwendet, erhält man die Gröbner-Basis 
\begin{equation}
  G
  =
  \{
    x^4 + x - 3,
    x^2 - y
  \},
  \label{buchberger:polynom-gleichungssystem-5}
\end{equation}
woraus sich das Gleichungssystem
\begin{equation}
  \left\{\quad
  \renewcommand{\arraycolsep}{1.5pt}
  \begin{array}{rcrcrcr}
  x^4 &+& x &-& 3&=& 0 \\
  x^2 &-& y & &  &=& 0
  \end{array}
  \right.
  \label{buchberger:polynom-gleichungssystem-6}
\end{equation}
konstruieren lässt.
\end{beispiel}

\subsubsection{Ausgabe des Buchberger-Algorithmus}
Das Gleichungssystem~\eqref{buchberger:polynom-gleichungssystem-6} ist zwar noch nicht gelöst, aber es besitzt eine nützliche Eigenschaft:
Die erste Gleichung beinhaltet nur noch die Variable $x$.
Eine Gleichung mit nur einer Variablen lässt sich numerisch lösen z.~B. mit dem \emph{Newton-Verfahren}.
Wie beim Gauss-Algorithmus erhält man beim Buchberger-Algorithmus eine Zeilenstufenform, 
wobei sich immer die Ergebnisse der vorherigen Gleichungen in die nächste einsetzen lassen.
Weil es sich hier um nichtlineare Gleichungen handelt, kann eine Gleichung mehrere Lösungen für eine Variable ergeben.
Beim Rückwärtseinsetzen müssen dann alle Kombinationen der bisherigen Lösungen eingesetzt werden.

\subsection{Funktionsweise des Buchberger-Algorithmus}

\begin{algorithm}[t]
\caption{Pseudocode des Buchberger-Algorithmus in C++/Java ähnlichem Stil\raisebox{1pt}{\strut}%
\label{buchberger:pseudo-code}}
\begin{algorithmic}[1]
  \State \textbf{function} Buchberger(Menge$<$Polynom$>$ startPolynome) \{
    \State \quad Menge$<$Polynom$>$ basis = startPolynome;
    \State \quad Menge$<$Polynom$>$ alteBasis = \{\};
    \State \quad \textbf{while} (basis $\neq$ alteBasis) \{
      \State \quad \quad alteBasis = basis;
      \State \quad \quad \textbf{for} (Pair$<$Polynom$>$ (p, q) $\in$ alteBasis) \{
        \State \quad \quad \quad Polynom rest = spezialDivision((p, q), alteBasis);
        \State \quad \quad \quad \textbf{if} (rest $\neq$ 0) \{
          \State \quad \quad \quad \quad basis = basis $\cup$ \{rest\}; \Comment{Fügt neues Polynom zur Basis hinzu.}
        \State \quad \quad \quad \} % end if
      \State \quad \quad \} % end for
    \State \quad \} % end while
    \State \quad \textbf{return} basis;
  \State \} % end function
\end{algorithmic}
\end{algorithm}

Dieser Abschnitt soll dem Leser eine Idee geben, wie der Buchberger-Algorithmus funktioniert.
Wie und warum der Algorithmus genau funktioniert, erklärt Edmund Lamagna in seinem Buch~\cite{buchberger:computer-algebra} in Kapitel 8.

Der Pseudocode des Algorithmus~\ref{buchberger:pseudo-code} zeigt vereinfacht den Ablauf auf.
Die Funktion \texttt{spezialDivision}, die in Zeile 7 benutzt wird, ist in sich selbst ein komplizierter Algorithmus und wird im Folgenden nur grob erklärt.
Der Algorithmus startet mit einer Menge von Polynomen, welche die Basis genannt wird (Zeile 2).
Aus den Polynomen der Basis werden alle möglichen Paare gebildet (Zeile 6).
Mit einer speziellen Polynomdivision \texttt{spezialDivision}\footnote{Die Funktion, die hier \texttt{spezialDivision} genannt wird, besteht eigentlich aus zwei Schritten:
  Die Funktion bildet zuerst ein sogenanntes S-Polynom aus dem Polynom-Paar \texttt{(p, q)} und führt darauf eine spezielle Polynomdivision aus,
  wobei nicht durch ein einzelnes Polynom dividiert wird, sondern durch eine Menge von Polynomen, hier die \texttt{alteBasis}.
  Dabei wird iterativ versucht, den führenden Term des S-Polynoms durch den führenden Term eines der Polynome der Basis zu dividieren.
  Sobald es nicht mehr möglich ist, eine solche Division ohne Rest durchzuführen, endet \texttt{spezialDivision}.
  Mehr dazu in~\cite{buchberger:computer-algebra}.
} (Zeile 7), die jeweils eines der Paare als Dividend und die ganze Basis als Divisor einbezieht, wird ein Quotient mit Rest berechnet.
Dabei will der Algorithmus herausfinden, ob jeweils zwei Polynome als Paar eine Information tragen, die in der Basis noch nicht enthalten ist.
Darum sind auch an einer dieser Polynomdivision-Operationen immer ein Paar und die ganze Basis beteiligt.

Die neue Information, die bei dieser Polynomdivision gewonnen wurde, ist der Rest der Division.
Dieser Rest wird als Polynom zur Basis hinzugefügt, um somit die neu gewonnene Information zu behalten (Zeile 9).
Durch die Vergrösserung der Basis haben sich auch neue Polynom-Paare ergeben.
Diese neuen Paare werden ebenfalls den Polynomdivisions-Schritt durchlaufen.

Wenn ein Paar keine neuen Informationen liefert, zeigt sich das daran, dass das Paar durch die Basis ohne Rest teilbar ist.
Wenn der Rest $0$ ist, war das Paar nutzlos (Zeile 8).
Die Tatsache, dass das Polynom $0$ keine Information liefert, ergibt durchaus Sinn:
Wenn man das Polynom $0$ als Gleichung $0 = 0$ interpretiert, erhält man eine Gleichung, die immer wahr ist und somit keine Information liefert.

Die Prozedur wird so lange wiederholt, bis keine neuen Polynome zur Basis hinzugefügt werden können (Zeile 4).
Das Resultat ist eine Basis mit mindestens so vielen Polynomen wie die ursprüngliche Basis (Zeile 13).
Der Grund ist, dass Polynome nur hinzugefügt, aber nie entfernt wurden.

Im letzten Schritt, der hier nicht genauer erklärt wird und auch nicht im Pseudocode~\ref{buchberger:pseudo-code} enthalten ist, werden redundante Polynome entfernt und das Ergebnis ist eine sogenannte \emph{Gröbner-Basis}.


\subsection{Visualisierung eines Gleichungssystems}
\label{buchberger:subsection:visualisierung}
\begin{figure}
\centering
\includegraphics[width=0.75\textwidth]{papers/buchberger/images/equationSystem1.png}
\caption{Visualisierung des Gleichungssystems \protect\eqref{buchberger:polynom-gleichungssystem-1}
  \textcolor{red}{Rot: $x + y^2 = 3$},
  \textcolor{blue}{Blau: $x^2 -y = 0$}.
  \label{buchberger:equationVisual1}}
\end{figure}

Der Plot~\ref{buchberger:equationVisual1}
visualisiert das Gleichungssystem \eqref{buchberger:polynom-gleichungssystem-1}.
Alle Kombinationen von $x$ und $y$, die dazu führen, dass eine Gleichung wahr wird, entsprechen einem Punkt auf dem roten bzw. blauen Graphen.
Die Schnittpunkte der Graphen ergeben gerade die Lösungen des Gleichungssystems.

\begin{figure}
\centering
\includegraphics[width=0.75\textwidth]{papers/buchberger/images/equationSystem2.png}
\caption{Visualisierung des Gleichungssystems \protect\eqref{buchberger:polynom-gleichungssystem-6}
  \textcolor{red}{Rot: $x^4 + x - 3 = 0$},
  \textcolor{blue}{Blau: $x^2 - y = 0$},
  \textcolor{gray}{Grau: $x + y^2 = 3$}.
\label{buchberger:equationVisual2}}
\end{figure}
Auf dem Plot~\ref{buchberger:equationVisual2}
ist das Gleichungssystem der Gröbner-Basis \eqref{buchberger:polynom-gleichungssystem-6}
dargestellt.
Die Eigenschaft, dass die erste Gleichung nur die Variable $x$ beinhaltet, wird hier auf interessante Weise sichtbar:
Der Graph der Gleichung besteht nur noch aus vertikalen Geraden.
Das erklärt sich dadurch, dass die Gleichung nicht mehr von $y$ abhängt und somit die $y$-Achse für die Gleichung nicht mehr relevant ist.
Die Gleichung wurde sozusagen auf eine Dimension reduziert.
Die beiden Lösungen haben sich nicht verändert, was ersichtlich ist, da die alte Gleichung, welche grau eingezeichnet ist, die gleichen Schnittpunkte wie die neuen Gleichungen hat.
